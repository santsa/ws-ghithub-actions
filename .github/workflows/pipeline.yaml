name: Pipeline
run-name: Deploy in live by ${{ github.actor }}

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
    paths:
      - "*.md"    

  env:
<<<<<<< HEAD
    FULL_NAME: ${{ github.actor }}-${{ github.ref_name }}
=======
    FULL_NAME: ${{ github.actor}}-${{ github.ref_name}}
>>>>>>> c852c476efb77312af3ed134e2382d5ba3cf95f0
    ENVIRONMENT: default

jobs:
  job1:
    environment: ${{ github.ref_name == 'main' && 'production' || 'develop' }}
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: ${{ github.ref_name == 'main' && 'production' || 'develop' }}
    steps:
      - name: Print message job1
        run: echo "Hello, Algo-${{ vars.NAME }}! ${{ env.ENVIRONMENT }}"

      - name: ECHO SECRETO
        run: echo "${{ secrets.SECRETO }}" > secreto1.txt      

      - name: List files
        run: ls -la
      
